// Hero section setup

export function setupHero() {
    const heroBg = document.querySelector(".hero-bg");
    
    // Set Hero background
    heroBg.style.backgroundImage = `url("../../assets/img/hero-bg.png")`;
    heroBg.style.opacity = 0.4;
}